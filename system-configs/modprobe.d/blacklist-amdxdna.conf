# Prevent auto-loading of AMD XDNA (NPU) driver on udev events.
# Caused SMU death spiral on 2026-02-28 (Steam removal → udev reload → SMU hang).
# Manual `modprobe amdxdna` still works — blacklist only blocks auto-detection.
blacklist amdxdna
